# Requirements for Raspberry Pi 5 Object Detection Stream
# Install with: pip install -r requirements_pi5.txt

# Core dependencies
opencv-python>=4.8.0
onnxruntime>=1.16.0
numpy>=1.24.0

# Optional: For better performance on Pi 5
# onnxruntime-gpu  # If using GPU acceleration (requires compatible setup)
